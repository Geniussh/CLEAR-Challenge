#!/usr/bin/env bash

# Setup
DIR="$( cd "$( dirname "$0" )" && pwd )"
mkdir -p $DIR/cl_ext_mem
mkdir -p $DIR/submissions

echo "Downloading CLEAR10 dataset (train/validation set)..."

echo "Unzipping data..."

mv 
